#!/bin/bash
# Draft — Codex session-end hook (no-op)
# Synthesis is now handled by the periodic Codex scanner in the daemon.
# This hook is retained for backwards compatibility with existing installs.
exit 0
