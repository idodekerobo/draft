{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install --frozen-lockfile"
  },
  "deploy": {
    "startCommand": "bun run backend/src/server.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
