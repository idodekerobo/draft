#!/usr/bin/env bash
# draft — entry point wrapper
# Delegates to the TypeScript CLI via bun.
# Copy to ~/bin/draft or /usr/local/bin/draft for global access.
exec bun run "$(dirname "$0")/cli/src/index.ts" "$@"
