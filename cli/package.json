{
  "name": "draft-cli",
  "version": "0.1.0",
  "description": "Draft CLI — control plane for the Draft daemon",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile ../draft-bin",
    "test": "bun test"
  },
  "dependencies": {
    "draft-core": "workspace:*",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9"
  }
}
