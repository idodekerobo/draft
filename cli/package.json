{
  "name": "draft-cli",
  "version": "0.1.0",
  "description": "Draft CLI — hosted control plane client",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run scripts/build.ts",
    "test": "bun test"
  },
  "dependencies": {
    "draft-core": "workspace:*",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9",
    "bun-types": "^1.3.14"
  }
}
